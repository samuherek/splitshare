import * as React from 'react';
import { RouteComponentProps } from '@reach/router';

export default class Bills extends React.PureComponent<RouteComponentProps> {
  public render() {
    return (
      <div>
        <div>Bills</div>
      </div>
    );
  }
}
