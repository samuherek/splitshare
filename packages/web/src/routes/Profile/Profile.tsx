import * as React from 'react';
import { RouteComponentProps } from '@reach/router';

export default class Profile extends React.PureComponent<RouteComponentProps> {
  public render() {
    return (
      <div>
        <div>Profile</div>
      </div>
    );
  }
}
